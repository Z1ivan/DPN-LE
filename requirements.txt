# Core dependencies
torch>=2.0.0
transformers>=4.35.0
numpy>=1.24.0

# Data processing
pandas>=2.0.0
xlrd>=2.0.1
openpyxl>=3.1.0

# Progress bars
tqdm>=4.65.0

# HuggingFace model loading/tokenizers
accelerate>=0.24.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# Optional experiment dependencies are exposed as package extras:
#   pip install -e '.[vllm]'     # general-capability vLLM evaluation
#   pip install -e '.[scoring]'  # GPT-based PersonalityBench scoring
